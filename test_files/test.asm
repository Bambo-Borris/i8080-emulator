; mov a, 10
; mov b, 10 ; Test a comment
; add a, b
; ; Test another comment
; sub a, b

; Check load
load b, 20 ; Check inline comment
load a, 0x50
; Check Add
add a, 1
add b, 2
; Check store
store a, 0x200
store b, 0x300
